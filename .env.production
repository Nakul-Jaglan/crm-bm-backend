# Production Environment Variables for Render
# This file should be uploaded as environment variables in Render dashboard

# Database Configuration (Render PostgreSQL)
DATABASE_URL=postgresql://your_db_user:your_db_password@your_db_host:5432/your_db_name

# Security - Generate a new one using: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=your-production-secret-key-64-characters-long-change-this

# Environment
ENVIRONMENT=production

# SMTP Configuration (Optional - for email notifications)
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# CORS Origins (will be set in config.py based on environment)
FRONTEND_URL=https://your-frontend-domain.vercel.app
